#Devops NGINX Demo

#This project will help you to install NGINX

#steps

sudo apt-get update

sudo apt install nginx

sudo systemctl start nginx

sudo systemctl enable nginx


webpage is kepy at /var/www/html
